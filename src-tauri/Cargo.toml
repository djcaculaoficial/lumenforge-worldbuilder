[package]
name = "lumenforge"
version = "0.2.1"
description = "Lumenforge local-first cinematic story studio"
authors = ["Lumenforge"]
edition = "2021"
rust-version = "1.75"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde_json = "1"
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
